@page
@model IndexModel
@{
    ViewData["Title"] = "Profil";
    ViewData["ActivePage"] = ManageNavPages.Index;
}

<style>
    .btn-soil {
        color: #fff;
        background-color: #462E1A;
        border-color: #462E1A;
    }

        .btn-soil:hover, .btn-soil:focus {
            color: #fff;
            background-color: #3a2413;
            border-color: #3a2413;
        }

    .card-custom {
        border-radius: 1rem;
        box-shadow: 0 0.5rem 1rem rgba(70,46,26,0.08);
        border: 1px solid #e5e5e5;
    }
</style>

<div class="row justify-content-center">
    <div class="col-md-7 col-lg-6">
        <div class="card card-custom mt-4">
            <div class="card-body p-4">
                <h3 class="mb-4 text-center fw-bold text-success">
                    <i class="bi bi-person-circle me-2"></i>@ViewData["Title"]
                </h3>
                <partial name="_StatusMessage" for="StatusMessage" />
                <form id="profile-form" method="post" autocomplete="off">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3" role="alert"></div>
                    <div class="form-floating mb-3">
                        <input asp-for="Username" class="form-control" placeholder="Kullanıcı adınız" disabled />
                        <label asp-for="Username" class="form-label"></label>
                    </div>
                    <div class="form-floating mb-3">
                        <input asp-for="Input.PhoneNumber" class="form-control" placeholder="Telefon numaranız" />
                        <label asp-for="Input.PhoneNumber" class="form-label"></label>
                        <span asp-validation-for="Input.PhoneNumber" class="text-danger"></span>
                    </div>
                    <button id="update-profile-button" type="submit" class="w-100 btn btn-soil btn-lg d-flex align-items-center justify-content-center gap-2">
                        <i class="bi bi-save"></i>
                        Kaydet
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}